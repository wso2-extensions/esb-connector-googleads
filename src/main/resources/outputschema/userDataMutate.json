{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "title": "Response Schema for the User Data Mutate operation of GoogleAds",
  "description": "Schema representing the output of the user data mutate operation in GoogleAds API",
  "properties": {
    "payload": {
      "description": "Response for the `userDataMutate` method.",
      "type": "object",
      "properties": {
        "uploadDateTime": {
          "description": "The date time at which the request was received by API.",
          "type": "string"
        },
        "receivedOperationsCount": {
          "description": "Number of upload data operations received by API.",
          "type": "number"
        }
      }
    },
    "attributes": {
      "description": "Metadata about the operation.",
      "properties": {
        "statusCode": {
          "description": "HTTP status code of the API response."
        }
      }
    },
    "headers": {
      "description": "HTTP headers of the API response.",
      "properties": {}
    }
  }
}
