{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "title": "Response Schema for the Create Customer Client operation of GoogleAds",
  "description": "Schema representing the output of the create customer client operation in GoogleAds API",
  "properties": {
    "payload": {
      "description": "Response for the `createCustomerClient` method.",
      "type": "object",
      "properties": {}
    },
    "attributes": {
      "description": "Metadata about the operation.",
      "properties": {
        "statusCode": {
          "description": "HTTP status code of the API response."
        }
      }
    },
    "headers": {
      "description": "HTTP headers of the API response.",
      "properties": {}
    }
  }
}
